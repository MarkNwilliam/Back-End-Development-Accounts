from setuptools import setup, find_packages

setup(
    name="accounts",
    version="1.0.0",
    description="Accounts REST API Service",
    packages=find_packages(),
    include_package_data=True,
)
